/** Link DTO interface. */
export interface ILinkDTO {
    /** Link's name. */
    name: string;

    /** Link's URL. */
    url: string;
}

/** Social networks DTO interface. */
export interface ISocialDTO {
    /** GitHub URL. */
    github: string;

    /** LinkedIn URL. */
    linkedin: string;
}